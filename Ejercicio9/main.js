/* Número aleatorio (0 o 1): Se genera un número aleatorio entre 0 y 1 con Math.random() 
y posteriormente se redondea con Math.round() para que quede 0 o 1 */
console.log(Math.round(Math.random()));

/* Redondear PI a 3: Usando el método Math.floor() introduciendo el valor Math.PI como valor */
console.log(Math.floor(Math.PI));